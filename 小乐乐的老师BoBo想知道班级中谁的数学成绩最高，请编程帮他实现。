#include<stdio.h>

int main()
{
    int a,i,temp,result=0;
    scanf("%d",&a);
    for(i = 0; i < a; i++)
    {
        scanf("%d",&temp);
        if(temp > result)
            result = temp;
    }
    printf("%d",result);
    return 0;
}
